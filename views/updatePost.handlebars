{{!-- Update Post --}}
<div class="row mt-4">
  <div id="create-card" class="col-9 card dashboard-card mx-auto">
    <div class="create-stripe">
      <h3>Create a New Blog Post:</h3>
    </div>
    <form class="form new-blogPost-form">
      <div class="form-group">
        {{!-- <label for="blogPost-name">{{title}}</label> --}}
        <input class="form-input" type="text" id="blogPost-name" name="blogPost-name" placeholder="{{title}}" />
      </div>
      <div class="form-group">
        {{!-- <label for="blogPost-content">Content:</label> --}}
        <textarea class="form-input" id="blogPost-content" name="blogPost-content" placeholder="{{blog_text}}"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" id= "submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>
</div>


{{!-- Script --}}
<script src="/js/dashboard.js"></script>