  <div class="blogPost">
    <h3>{{blogPost.title}}</h3>
    <p>By: {{blogPost.user_id}}</p> <!-- Change this to user name-->
    <p>{{blogPost.date}}</p> <!-- Change this to user name-->
    <!-- The custom method 'format_date' is used to calculate the end date and format it correctly -->
    <p>{{ blogPost.blog_text}}</p>
  </div>



{{!-- FROM MINI PROJECT
<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div> --}}
